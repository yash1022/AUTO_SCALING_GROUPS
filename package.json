{
  "name": "auto-scaling-groups-server",
  "version": "1.0.0",
  "private": true,
  "description": "A simple Node and Express server with test routes",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}